name: commit_formatter
description: Helper for Git native hooks

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  path: ^1.7.0

dev_dependencies:
  effective_dart: ^1.3.0
  test: ^1.15.7
